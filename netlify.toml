# netlify.toml (Cleaned Version)

[build]
  # Next.js build command
  command = "next build"

# 🛑 REMOVE THE [functions] BLOCK ENTIRELY 🛑
# We will set the timeout using an environment variable instead.

# Explicitly use the Netlify Next.js Build Plugin
[[plugins]]
  package = "@netlify/plugin-nextjs"